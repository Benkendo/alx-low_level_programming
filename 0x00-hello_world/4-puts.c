# include <stdio.h>
/**
 * main - main function
 * char -this for character
 * int main(void) - is amain function
 * return(0) - returns everything after execution
 *return(mai)n- returns main function result
 *
 * Return() -return function
 */
int main(void)
{
char line[] = "Programming is like building a multilingual puzzle";
puts(line);
return (0);
}
